<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose owns all styling. This theme exists only so the window and the
      pre-Compose first frame match the app background rather than flashing white.
    -->
    <style name="Theme.KhaiDai" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/canvas</item>
    </style>
</resources>
